name: Daily Usage Update

on:
  schedule:
    - cron: '30 23 * * *'  # Runs at 23:30 UTC every day
  workflow_dispatch: # Allows manual trigger

jobs:
  update-usage:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run update script
        env:
          GITHUB_TOKEN: ${{ secrets.API_TOKEN }}
          GITHUB_USER : ${{ secrets.API_USER }}
        run: python scripts/update_usage.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/*.json data/**/*.json
          git commit -m "Update usage data $(date +'%Y-%m') [skip ci]" || echo "No changes to commit"
          git push
